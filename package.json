{
  "name": "mylibbb",
  "version": "1.0.0",
  "description": "Library inventory system. Created using MERN stack",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "test": "echo \"Error: no test specified\" && exit 1",
    "cleaner": "npx prettier --write '**/*.js'&&npx prettier --write '**/*.css'&&npx prettier --write '**/*.html'",
    "audits": "yarn install&&npm-update-all&&npm audit&&npm audit fix&& yarn audit",
    "predeploy": "npm run build",
    "deploy": "npm run build&&gh-pages -d build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Toughee/MyLibbb.git"
  },
  "author": "Tjdev7",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Toughee/MyLibbb/issues"
  },
  "homepage": "https://toughee.github.io/MyLibbb/",
  "dependencies": {
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "devDependencies": {
    "cypress": "^10.0.2",
    "gh-pages": "^4.0.0",
    "jest": "^28.1.0",
    "mocha": "^10.0.0",
    "typescript": "^4.7.3"
  }
}
